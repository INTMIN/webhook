{
  "name": "webhook",
  "version": "1.0.0",
  "description": "webhook",
  "main": "webhook.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "intmin",
  "license": "ISC",
  "dependencies": {
    "express": "^5.1.0"
  }
}
